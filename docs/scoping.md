# Admission CRM — Next.js Frontend Scoping Doc

Planning only — nothing scaffolded yet. This maps every backend module to a
frontend page, the exact generated hook each page calls, and the role gating
each page needs, then sequences the build.

## 0. Prerequisite already done: typed client

`Admission-CRM-API/typed-client/` now holds a generated TypeScript client —
types + TanStack Query v5 hooks for all 14 modules, generated by `orval` from
the live OpenAPI schema (`app.openapi()`, not a hand-maintained spec). Every
page below is built against these generated hooks, never a hand-typed
`fetch()` call. Regenerate with `npm run generate` inside that folder any
time the backend schema changes — never hand-edit anything under
`typed-client/src/generated/`.

## 1. Component / design system — decided once, here

**shadcn/ui + Tailwind CSS.** This is the standard pairing for a
Next.js + FastAPI stack and is decided at the project level, not per page:

- Tailwind for styling, shadcn/ui (Radix primitives + Tailwind, copied into
  the repo rather than installed as an opaque package) for the component
  layer — Table, Dialog, Form, Select, DatePicker, Badge, Tabs, Sidebar nav,
  Toast — all consistent by default, all editable in-repo since shadcn
  components live in your own `components/ui/` folder.
- TanStack Query v5 (already the target for the generated hooks) handles all
  server state — caching, refetch, optimistic updates on mutations.
- React Hook Form + Zod for every create/edit form, with Zod schemas
  generated or derived from the same OpenAPI types where practical, so form
  validation and API validation stay in sync.
- No separate state-management library (Redux/Zustand) needed at this
  scope — TanStack Query covers server state, and page-local UI state
  (open dialogs, selected filters) is plain `useState`.

## 2. Page / feature inventory — all 14 modules

Every module follows the same CRUD route shape:
`GET /{resource}` (paginated list), `GET /{resource}/{id}`,
`POST /{resource}`, `PATCH /{resource}/{id}`, `DELETE /{resource}/{id}`.
Role gating is identical across all 14 (from `app/core/jwt_auth.py`):
**read** = any authenticated role (admin/counselor/staff/viewer) — no
`viewer` block; **write** (create/update) = `require_writer`, blocks
`viewer` only; **delete** = `require_deleter`, admin/staff only.

| # | Module | Route prefix | Page | Generated hooks used | Read | Write | Delete |
|---|---|---|---|---|---|---|---|
| 1 | leads | `/leads` | Leads list + detail/edit drawer | `useListLeadsLeadsGet`, `useGetLeadLeadsLeadIdGet`, `useCreateLeadLeadsPost`, `useUpdateLeadLeadsLeadIdPatch`, `useDeleteLeadLeadsLeadIdDelete` | any role | not viewer | admin/staff |
| 2 | applicants | `/applicants` | Applicants list + profile page | `useListApplicantsApplicantsGet`, `useGetApplicantApplicantsApplicantIdGet`, `useCreateApplicantApplicantsPost`, `useUpdateApplicantApplicantsApplicantIdPatch`, `useDeleteApplicantApplicantsApplicantIdDelete` | any role | not viewer | admin/staff |
| 3 | applications | `/applications` | Applications list + detail (linked from applicant profile) | `useListApplicationsApplicationsGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 4 | fee_payments | `/fee-payments` | Fee ledger (per-applicant + global list) | `useListFeePaymentsFeePaymentsGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 5 | scholarships | `/scholarships` | Scholarships list + award/edit form | `useListScholarshipsScholarshipsGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 6 | hostel_allotments | `/hostel-allotments` | Hostel allotment board | `useListHostelAllotmentsHostelAllotmentsGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 7 | telecallers | `/telecallers` | Telecaller roster + performance tab | `useListTelecallersTelecallersGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 8 | call_schedules | `/call-schedules` | Call scheduling calendar/list | `useListCallSchedulesCallSchedulesGet` + CRUD equivalents (note: `scheduled_time` must be sent offset-aware — see Tier 2 timezone work already shipped) | any role | not viewer | admin/staff |
| 9 | campus_visits | `/campus-visits` | Campus visit log | `useListCampusVisitsCampusVisitsGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 10 | followups | `/followups` | Follow-up tracker | `useListFollowupsFollowupsGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 11 | counseling_sessions | `/counseling-sessions` | Counseling session log (has `topics_discussed` array field) | `useListCounselingSessionsCounselingSessionsGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 12 | document_types | `/document-types` | Admin: document type config table | `useListDocumentTypesDocumentTypesGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 13 | lookup_values | `/lookup-values` | Admin: lookup/dropdown value config table | `useListLookupValuesLookupValuesGet` + CRUD equivalents | any role | not viewer | admin/staff |
| 14 | settings | `/settings` | Admin: app settings (category/key/value) | `useListSettingsSettingsGet` + CRUD equivalents | any role | not viewer | admin/staff |

Plus non-module pages:

| Page | Backend | Notes |
|---|---|---|
| Login | `POST /auth/login` (`useLoginAuthLoginPost`) | Issues Supabase-backed access token; store per whatever session helper the app adopts (httpOnly cookie recommended over localStorage) |
| Dashboard | Aggregates GET-list calls across leads/applications/call_schedules/followups | Funnel counts, overdue alerts — same shape as the old Streamlit Dashboard page, rebuilt on real data instead of ad hoc queries |
| Reports | Same aggregation layer as Dashboard, exported client-side (CSV/PDF) or via a future dedicated export endpoint | Not a distinct backend module today — flagged as a gap to decide on later (client-side export vs. new API endpoint) |

Note: **Lead Scoring** and **AI Call Script**, from the original Streamlit
module table, have no corresponding backend module in the 14 — they're ML
features, not yet built anywhere (see the 10-module ML roadmap discussed
earlier). They're out of scope for this frontend build until the ML side
exists to call.

## 3. Auth/session wiring (applies to every page above)

- `typed-client/src/api-mutator.ts`'s `getAccessToken()` placeholder needs
  to be replaced with the real session read once the Next.js app exists —
  likely a server-side helper reading an httpOnly cookie set at login,
  rather than client-side `localStorage` (avoids XSS token theft).
- Role-based UI gating (hide/disable write and delete controls for
  `viewer`, hide delete for non-admin/staff) should mirror the API's own
  enforcement — the API is the real gate; the UI gating is only to avoid
  showing controls that would 403, not a security boundary itself.
- A shared route wrapper/middleware checks for a valid session on every
  page except `/login`, matching the API's "every endpoint requires a
  bearer token except `/health`" posture.

## 4. Build order

**Phase A — Core admissions funnel** (highest business value, most
frequently used, and validates the whole stack — API client, auth, forms,
tables — before spending time on the rest):
1. Leads
2. Applications (depends on Applicants existing as a linked entity —
   build Applicants alongside/just before)
3. Fee Payments

**Phase B — Config/lookup screens** (low complexity, no dependencies on
Phase A being finished, good for filling in team velocity or parallelizing
across two people):
4. Document Types
5. Settings
6. Lookup Values

**Phase C — Remaining operational modules** (each follows the exact same
CRUD template proven in Phase A, so these should be fast once the pattern
is established):
7. Applicants (if not already pulled forward for Applications' FK)
8. Scholarships
9. Hostel Allotments
10. Telecallers
11. Call Schedules
12. Campus Visits
13. Followups
14. Counseling Sessions

**Phase D — Dashboard/reporting** (last, because it aggregates data from
everything above — building it earlier means rebuilding it as more
modules land):
15. Dashboard
16. Reports/export

## 5. Explicitly out of scope for this doc

- Scaffolding the actual Next.js project (next step, once this doc is
  signed off).
- Lead Scoring / AI Call Script pages (no backend module yet).
- Deciding the session/cookie mechanism in code (flagged above as a
  decision to make when auth wiring is actually built).
- Client-side vs. server-side export approach for Reports (flagged as an
  open question, not blocking Phase A-C).

## 6. Known gaps (found during the build, not resolved yet)

- ~~**counselor_id on Counseling Sessions is a raw UUID text input, not a
  picker.**~~ **RESOLVED (Sept 2026).** The backend gained a read-only
  `GET /users` (paginated, active users only) and `GET /users/{id}`,
  returning only `id, full_name, role, department, is_active` - never email
  or phone. `?role=` is multi-value (repeated or comma-separated) so the
  caller decides which roles to offer; the Counseling Sessions picker asks
  for `admin, counselor, staff` (viewers excluded) via the generated
  `useListUsersUsersGet` hook, and the amber warning is gone. An
  already-saved counselor who is no longer in that list (deactivated, or now
  a viewer) is shown as an explicit "Current counselor (inactive or
  unavailable)" option when editing, rather than a blank picker.
  `GET /users` is deliberately GET-only: creating/deactivating staff means
  going through Supabase Auth (the `on_auth_user_created` trigger provisions
  `public.users`), which is a separate piece of work.
- **applicants.assigned_counselor is still a raw user-ID text input**
  (Applicants page, "Assigned counselor (user ID)"). It is the same kind of
  FK to `users(id)` and can now use the same `GET /users` picker; not
  changed in this pass because only `counselor_id` on Counseling Sessions
  was in scope.
- **The counselor picker is capped at the first 200 active users** (the
  API's max page size), same as the applicant/application pickers next to
  it. Fine at this scale; needs search or paging if the staff list ever
  approaches that.
